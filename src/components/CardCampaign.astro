---
const { title, description, money_objective, money, content, imageUrl } = Astro.props;
---
<div class="max-w-sm rounded-lg overflow-hidden bg-gray-100 shadow-lg">
    <div class="flex">
        <div class="flex-1 p-4">
            <h2 class="text-xl font-bold mb-2">{title}</h2>
            <p class="text-gray-700 mb-2"><b>Descripción:</b> {description}</p>
            <p class="text-gray-700 mb-2"><b>Objectivo:</b> {money_objective}</p>
            <p class="text-gray-700 mb-2"><b>Recaudado:</b> {money}</p>
        </div>
        <div class="flex-1 p-4">
            <img class="w-full h-48 object-cover" src={imageUrl} alt={title} />
        </div>
    </div>
</div>